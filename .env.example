# Skylapse Environment Configuration

# ============================================================================
# STORAGE PATHS
# ============================================================================
# Image storage path (transferred from Pi)
# Default: ./data/images
SKYLAPSE_IMAGES_PATH=./data/images

# Timelapse video output path
# Default: ./data/timelapses
SKYLAPSE_TIMELAPSES_PATH=./data/timelapses

# ============================================================================
# SERVICE PORTS
# ============================================================================
# Capture service (runs on Pi)
PI_PORT=8080

# Backend API service
BACKEND_PORT=8082

# Processing service
PROCESSING_PORT=8081

# Frontend development server
FRONTEND_PORT=3000

# ============================================================================
# PI CONNECTION
# ============================================================================
# Pi hostname or IP (use IP for Docker host networking)
PI_HOST=192.168.0.124

# Pi SSH username for transfer service
PI_USER=nicholasmparker

# Pi source directory for images
PI_SOURCE=~/skylapse-images/

# ============================================================================
# TRANSFER SERVICE
# ============================================================================
# Enable/disable image transfer from Pi to backend
# Set to false for development when another backend is handling transfers
# Set to true for production backend that should receive images
TRANSFER_ENABLED=true

# How often to sync images from Pi to backend (minutes)
# 5 min = near real-time, ~10 images per sync
TRANSFER_INTERVAL_MINUTES=5

# Delete Pi images older than X days (after successful backup)
# 1 day keeps Pi storage lean
DELETE_AFTER_DAYS=1

# Destination directory inside transfer container
LOCAL_DEST=/data/images

# ============================================================================
# BACKEND SERVICE
# ============================================================================
# Base URL for backend API (used for image URLs, webhooks, etc)
# Production: https://skylapse.example.com
# Development: http://localhost:8082
BACKEND_URL=http://localhost:8082

# ============================================================================
# FRONTEND SERVICE
# ============================================================================
# Backend URL for frontend to connect to
# Must be accessible from browser
VITE_BACKEND_URL=http://localhost:8082
