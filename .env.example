# Skylapse Environment Configuration

# Image storage path (transferred from Pi)
# Default: ./data/images
SKYLAPSE_IMAGES_PATH=./data/images

# Timelapse video output path
# Default: ./data/timelapses
SKYLAPSE_TIMELAPSES_PATH=./data/timelapses

# Pi configuration
PI_HOST=helios.local
PI_PORT=8080

# Transfer service configuration
TRANSFER_INTERVAL_MINUTES=5  # How often to sync images (5 min = near real-time)
DELETE_AFTER_DAYS=1           # Delete Pi images older than X days (after backup)
